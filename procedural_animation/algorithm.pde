// Algorithm.pde

abstract class Algorithm {
  // Initialize the algorithm
  abstract void init();
  
  // Update the algorithm's state
  abstract void update();
  
  // Display the algorithm's visualization
  abstract void display();
}
